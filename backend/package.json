{
  "name": "pwa-tareas-03-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend de la aplicación: API HTTP para el formulario de contacto y la subida de archivos.",
  "main": "src/servidor.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/servidor.js",
    "dev": "node --env-file-if-exists=.env --watch src/servidor.js",
    "test": "node --env-file-if-exists=.env --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.4.0",
    "pg": "^8.23.0"
  }
}
